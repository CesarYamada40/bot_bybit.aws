{
  "name": "bot_bybit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx ng build --configuration production",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@angular/cli": "^16.0.0"
  }
}
